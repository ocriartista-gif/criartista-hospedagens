import { leads } from "@/lib/mock-data";
export default function LeadsPage(){return <><header className="admin-header"><div><span className="eyebrow">CRM</span><h1>Leads</h1><p>Origem, estágio comercial e próximos passos em um só lugar.</p></div></header><section className="admin-panel"><table><thead><tr><th>Nome</th><th>Check-in</th><th>Acomodação</th><th>Origem</th><th>Status</th></tr></thead><tbody>{leads.map((lead)=><tr key={lead.id}><td>{lead.name}<small>{lead.whatsapp}</small></td><td>{lead.checkIn}</td><td>{lead.accommodationId ?? "—"}</td><td>{lead.source}</td><td><span className={`badge badge-${lead.status}`}>{lead.status.replaceAll("_"," ")}</span></td></tr>)}</tbody></table></section></>}
