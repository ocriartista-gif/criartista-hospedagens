import { reviews } from "@/lib/mock-data";
export default function ReviewsPage(){return <><header className="admin-header"><div><span className="eyebrow">Prova social</span><h1>Avaliações</h1><p>Depoimentos reais cadastrados individualmente.</p></div><button className="button button-primary">+ Nova avaliação</button></header><div className="admin-card-grid">{reviews.map((review)=><article className="admin-panel" key={review.id}><div className="stars">{"★".repeat(review.rating)}</div><h2>{review.guestName}</h2><p>“{review.text}”</p><div className="meta-row"><span>{review.source}</span><span>{review.featured?"Destaque":"Normal"}</span><span>{review.published?"Publicado":"Oculto"}</span></div><button className="button button-secondary">Editar</button></article>)}</div></>}
